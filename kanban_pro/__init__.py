"""kanban-pro: a backend-agnostic kanban proxy (ports & adapters).

One canonical kanban model + a KanbanBackend port; each backend is a swappable
adapter. See SPEC.md for the authoritative design.
"""
